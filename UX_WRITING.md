# UX Writing

이 프로젝트에서 사용자에게 보이는 문구는 토스 UX 라이팅 원칙에 맞춰 써요.

## 적용 범위

- `ui/src/**`에 있는 화면 문구
- `server.cjs`와 런타임 모듈에서 UI가 그대로 보여주는 `message`, `title`, `description`, `summaryKo`, `lastMessage` 같은 문자열

## 기본 원칙

- 해요체로 써요
- 능동형으로 말해요
- 부정문보다 사용자가 다음 행동을 떠올릴 수 있는 긍정형을 먼저 써요
- `~시`, `~께`, `여쭈다` 같은 과한 경어는 쓰지 않아요
- `{명사} + {명사}` 형태보다 풀어쓴 동사형 문장을 우선해요
- 한 줄 안에서 이해할 수 있게 짧게 써요
- 버튼은 행동이 바로 보이도록 동사형으로 써요
- 문장 끝 온점과 말줄임표는 쓰지 않아요

## 권장 표현

- `불러오는 중입니다` 대신 `불러오고 있어요`
- `설정이 완료되었습니다` 대신 `설정을 마쳤어요`
- `사용할 수 없습니다` 대신 `연결하면 사용할 수 있어요`
- `재시도하시겠어요?` 대신 `다시 시도할까요?`

## 예외

- 탭명, 표 헤더, 상태 배지처럼 매우 짧은 라벨은 명사형을 유지할 수 있어요
- `OpenClaw`, `OAuth`, `API`, `JSON`, `CLI`, 명령어, 코드값, 환경 변수 이름은 그대로 써요
- 기술적 정확성이 더 중요한 경우에는 용어를 번역하지 않아요

## 작업 체크리스트

- 새 문구를 넣을 때 `npm run test:ux-writing`를 먼저 돌려요
- 어색한 해요체가 보이면 단순 치환 대신 문장을 다시 써요
- 에러 문구는 실패 사실만 말하지 말고 가능한 다음 행동도 함께 안내해요
